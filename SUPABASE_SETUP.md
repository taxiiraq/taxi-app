# إعداد Supabase للتطبيق

## الخطوات المطلوبة:

### 1. إنشاء مشروع Supabase
1. اذهب إلى [supabase.com](https://supabase.com)
2. سجل دخول أو أنشئ حساب جديد
3. اضغط على "New Project"
4. اختر اسم للمشروع (مثل: taxi-app-db)
5. أدخل كلمة مرور قوية لقاعدة البيانات
6. اختر المنطقة الأقرب لك
7. انتظر حتى يكتمل إنشاء المشروع

### 2. الحصول على بيانات الاتصال
1. في لوحة التحكم، اذهب إلى Settings > API
2. انسخ:
   - **Project URL** (مثل: https://your-project.supabase.co)
   - **anon public key** (مثل: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...)

### 3. إنشاء جداول قاعدة البيانات
1. اذهب إلى SQL Editor في لوحة التحكم
2. انسخ محتوى ملف `supabase/schema.sql`
3. الصق الكود في المحرر واضغط "Run"

### 4. إعداد متغيرات البيئة
1. أنشئ ملف `.env` في مجلد المشروع
2. أضف المتغيرات التالية:

```env
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

### 5. تثبيت التبعيات
```bash
npm install
```

### 6. اختبار الاتصال
1. شغل التطبيق: `npm start`
2. جرب تسجيل حساب جديد
3. جرب تسجيل الدخول

## ملاحظات مهمة:

### Row Level Security (RLS)
- تم تفعيل RLS على جميع الجداول
- المستخدمون يمكنهم الوصول فقط لبياناتهم
- المديرون يمكنهم الوصول لجميع البيانات

### الإشعارات التلقائية
- يتم إنشاء إشعارات تلقائياً عند تغيير حالة الطلب
- الإشعارات تُرسل للعملاء عند:
  - قبول الطلب
  - بدء التوصيل
  - إكمال التوصيل

### الوظائف المضافة
- `handle_new_user()`: إنشاء مستخدم في جدول users عند التسجيل
- `create_notification()`: إنشاء إشعارات
- `handle_order_status_change()`: إدارة تغييرات حالة الطلب

## استكشاف الأخطاء:

### مشاكل الاتصال
- تأكد من صحة URL و API Key
- تأكد من أن المشروع نشط في Supabase

### مشاكل المصادقة
- تأكد من تفعيل Email Auth في Authentication > Settings
- تأكد من إعداد RLS policies بشكل صحيح

### مشاكل الجداول
- تأكد من تشغيل schema.sql بنجاح
- تحقق من وجود جميع الجداول في Table Editor

## الأمان:
- جميع البيانات محمية بـ RLS
- كلمات المرور مشفرة تلقائياً
- API Keys محدودة الصلاحيات
- يمكن إضافة المزيد من القيود حسب الحاجة 